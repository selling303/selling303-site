# Deploy Queue

Changes waiting to be pushed to production. Each conversation logs what it changed here. When Jacob approves a deploy, summarize everything below, push, then clear the list.

---

_(queue cleared after 2026-05-17 PM production deploy — see DEPLOY_LOG.md)_

## Pending

- **2026-05-17 [SEO/AEO — Review schema completeness on success stories + homepage]:** Audit of GSC's "Item Name N/A" column on Breadcrumb / FAQ / Review reports surfaced real gaps in Review markup (the BreadcrumbList/FAQPage N/A is harmless; the Review side had actual issues). Two fixes in this commit. (1) **`src/pages/sell/success-stories/[slug].astro` — LocalBusiness wrapper completeness.** Previous schema had `name`, `@id`, `review[]`, `aggregateRating` only — missing the fields Google requires for Review rich result snippet eligibility on a LocalBusiness wrapper. Added `url`, `telephone` (+1-303-997-0634), `image` (Jacob's headshot), `priceRange` ($$$), `address` (8z Real Estate office in Greenwood Village), and `openingHoursSpecification` (Mon-Sun 08:00-20:00). Per-Review enrichment added `datePublished` (from frontmatter `closeDate`), `itemReviewed` (@id ref to LocalBusiness), and `publisher` (@id ref to Organization). Net: 15 success-story pages × ~2 testimonials each = ~30 Review entries now Google-eligible for star-rating rich result display in search results. (2) **`src/pages/index.astro` — homepage Review array backing the existing AggregateRating.** Existing homepage RealEstateAgent block already had `aggregateRating` (5.0, reviewCount 47) but no `review` array to back it. Added a second JSON-LD block with same `@id` (`https://selling303.com/#agent`) carrying a `review` array dynamically built from all published success stories' testimonials (filtered via `getCollection('successStories', e => !e.data.draft)`). Each homepage Review carries the same datePublished + itemReviewed + publisher fields. Google merges multiple JSON-LD blocks by `@id`, so the agent profile now has both aggregate AND backing reviews on the homepage. Verified: all 15 success-story md files have `closeDate` in frontmatter. Frontmatter JS syntax-checked clean. Build verification on next Netlify deploy.
