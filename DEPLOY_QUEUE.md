# Deploy Queue

Changes waiting to be pushed to production. Each conversation logs what it changed here. When Jacob approves a deploy, summarize everything below, push, then clear the list.

---

_(queue cleared after 2026-05-17 production deploy — see DEPLOY_LOG.md)_

## Pending

- **2026-05-17 [SEO — "Crawled, not indexed" bucket: 6 silent 404s migrated from dead root file]:** Investigating the GSC "Crawled – currently not indexed" bucket (9 URLs) revealed that 5 old Framer redirects (`/zillow-step-2`, `/james-hendry`, `/jacci-mason`, `/steven-brooks`, `/page`) existed ONLY in the dead root `/_redirects` file (deleted earlier today, commit `f8dcea3`). Since Astro doesn't copy that file to `dist/`, those rules never fired on prod — the URLs have been silently 404'ing the entire time, which is why they ended up in "Crawled – currently not indexed" with last-crawled dates as old as December 2025. Also discovered 1 missing listing-address redirect: `/7327-s-carr-ct-littleton-co-80128` → 404 on prod, despite the success story existing at `/sell/success-stories/7327-s-carr-ct-littleton`. Added all 6 to the LIVE `public/_redirects`. Also added 3 belt-and-suspenders redirects from the dead root file that haven't surfaced in GSC yet but were intended to redirect: `/404-page`, `/party-in-medema-park` → `/`, and `/Denver-Metro` → `/neighborhoods`. Of the remaining 3 URLs in the GSC bucket, none need action: `/contact/` and `/?amp=1` are correctly canonicalized (the canonical-tagged version is indexed; Google dedupes the trailing-slash + AMP variants — expected behavior), and `www.selling303.com/blog/best-time-to-sell-home-littleton-co` already redirects through 2 hops to `/blog/relist-home-littleton-after-expired-listing` and will clear on re-crawl. After live deploy, click "Validate Fix" on the bucket — once Google re-crawls and sees the 9 URLs now resolve (6 via new 301s, 3 via canonical dedup), the bucket should drop to 0.

- **2026-05-17 [CORRECTION — _redirects file gotcha + actual fix]:** Earlier same-day commits `2865e36` and `6d6d54b` modified `/_redirects` at repo root — that file is DEAD CODE. Astro copies `public/*` verbatim to `dist/`, and Netlify reads `dist/_redirects`, so the **live `_redirects` is `public/_redirects`**. Commits to the root file were no-ops in production. Discovered by spot-checking why old-blog 301s like `/blog/is-2025-a-good-year-...` were already routing to specific successor posts despite no entry in root `/_redirects` — they were in `public/_redirects` all along. Re-applied all fixes to `public/_redirects`: (a) 7 published listing addresses → specific success-story slugs; (b) 6 draft listing addresses → `/sell/success-stories` hub; (c) 2 `.html` blog URLs → extension-less slugs; (d) updated `south-denver-suburbs-community-happenings` from `/blog` to `/neighborhoods`. Also reordered so the 2 `.html` rules sit ABOVE the `/blog/:qGAS1Evcm` catch-all (Netlify processes rules top-to-bottom; first match wins, and `:qGAS1Evcm` is a placeholder that matches any single segment under `/blog/`, so it would have swallowed the `.html` rules if placed below). Replaced root `/_redirects` with a neutralized stub that warns future sessions to edit `public/_redirects` instead.

- **2026-05-17 [SEO — "Page with redirect" cleanup in GSC]:** GSC reports 49 URLs as "Page with redirect" with a Validation Failed banner (started 4/14, failed 4/17). Pulled the full list from the coverage drilldown export. Most are working as intended (16 `www.` → apex, 2 HTTP → HTTPS, several legacy Framer URLs already redirecting to current canonical pages, ~5 old blog slugs already pointing at smart specific destinations) — those stay as-is. The actionable subset = **13 old Framer listing-address URLs 301'ing to the generic `/properties` hub** and **2 old `.html` blog URLs 301'ing to the generic `/blog` hub** — both bleed SEO equity by sending Google to a hub instead of the matching content. Rewrote `_redirects` to map each old URL to its specific destination: (1) **7 published success-story addresses** — `14556-w-3rd-ave-golden-co-80401`, `2993-s-jericho-ct-aurora-co-80013`, `6566-s-flower-st-littleton-co-80123`, `6718-s-holland-way-littleton-co-80128`, `6720-s-holland-way-littleton-co-80128`, `8965-e-phillips-dr-centennial-co-80122`, `21069-woodside-lane-parker-co-80138` → `/sell/success-stories/<slug>` (all 7 destinations verified 200 OK on prod). (2) **6 draft success-story addresses** — `8781-flora-ct-arvada`, `22461-e-union-cir-aurora`, `10315-ravenswood-lane-highlands-ranch`, `301-w-lehow-ave-14b-englewood`, `9559-w-coal-mine-avenue-unit-a-littleton`, `4360-w-wagon-trail-dr-denver` → `/sell/success-stories` hub (drafts have noindex meta + are filtered from sitemap per `DRAFT_SUCCESS_STORY_SLUGS` in astro.config, so the hub is the best indexable destination for now; will update to specific URLs once those stories are completed). (3) **2 `.html` blog URLs** — `closing-costs-colorado-buyers-2026.html` and `why-homes-sit-on-market-south-denver.html` → extension-less Astro slugs (both destinations verified 200). Replaces the previous `/6718-s-holland-way-littleton-co-80128 /properties 301` and `/4360-w-wagon-trail-dr-denver-co-80123 /properties 301` rules. (4) **1 deleted blog post** — `south-denver-suburbs-community-happenings-mid-october-2025` (community-roundup content, no successor post) → `/neighborhoods` hub instead of `/blog` (topically tighter — the post was about neighborhood happenings). Net effect: 16 of the 49 "Page with redirect" URLs now pass SEO equity through to the right destination instead of dumping to a generic hub. Once Google re-crawls and re-validates, the "Validation Failed" banner should flip and these URLs drop out of the "Page with redirect" bucket. Other 33 URLs in the bucket are expected normalizations (www, HTTPS, legacy → current canonical) and stay. **Follow-up:** after next live deploy, click "Validate Fix" on the "Page with redirect" report in GSC.
