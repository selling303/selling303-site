# Deploy Queue

Changes waiting to be pushed to production. Each conversation logs what it changed here. When Jacob approves a deploy, summarize everything below, push, then clear the list.

---

_(queue cleared after 2026-05-17 PM production deploy — see DEPLOY_LOG.md)_

## Pending

- **2026-05-17 [GSC fix — duplicate Dataset on NOV protest playbook]:** GSC URL Inspection on `/blog/2026-notice-of-valuation-protest-playbook-south-denver` flagged "2 items detected: Some are invalid" with a critical "Missing field description" error. Root cause: the page emitted two Dataset entities — (1) a clean JSON-LD `<script>` block, and (2) inline microdata on the `<div id="gc-widget">` wrapper (`itemscope itemtype="https://schema.org/Dataset"` + two `<meta itemprop>` tags) that only declared `name` and `temporalCoverage`. Google read the widget microdata as a second, broken Dataset. Fix: stripped `itemscope itemtype` and the two `<meta itemprop>` lines off `#gc-widget` so only the JSON-LD survives. Also added `license: https://creativecommons.org/licenses/by-nd/4.0/` to the JSON-LD to clear the non-critical optional-license warning on the surviving entity. Net: page should now show "1 valid item" with zero issues on next Live Test.

- **2026-05-17 [New blog post — Day 14 of content calendar]:** "Why Parker Move-Up Listings Stall — April 2026 Data" (`src/content/blog/parker-move-up-listings-stall-pricing-strategy-2026.md`). Expired Listings pillar + Parker geographic pillar. BOFU seller advice. Diagnostic for stalled Parker listings in the $800K–$1.2M move-up bracket, the relist pricing formula tuned to Parker DOM benchmarks, and the recovery timeline. Built on April 2026 REcolorado MLS data (181 closed at 11d median DIM / 32 expired at 68d / 9 expired in $800K–$1.2M bracket at 95d) + DMAR April 2026 metro context + Freddie Mac rate context. Shipped with **single-metric-bar-chart Tier 0** visual (3-bar median DIM comparison, full Dataset JSON-LD + bound figcaption source row). Title shortened from calendar entry to "Why Parker Move-Up Listings Stall — April 2026 Data" (51 chars) for SERP CTR; original preserved in `headline` frontmatter. blog-review PASS on all 10 dimensions; readability PASS (avg 18.4 / p90 33.0 / Flesch 51.7 / FK 10.6). Bookkeeping: content-calendar-2026.md marked [x] (drafted 2026-05-17), content-cluster-map.md updated under Expired Listings cluster, visual-inventory.md updated with the new entry.

- **2026-05-18 [Dead-infra fix — dynamic OG image system + Schema.org + sitemap redirect (Chunk A of dead-infra audit)]:** Dead-infrastructure audit on 2026-05-17 found every page's `og:image` was 404'ing in production (referenced `/images/og-*.jpg` paths at the dead repo root, never reached `dist/`). Affected social previews (Facebook, LinkedIn, iMessage, Slack) site-wide and broke Schema.org `Person.image` + `Organization.logo` on the homepage. This commit fixes the entire chain.
  - **New build-time OG generation system.** Added `src/lib/og.ts` (Satori + Resvg pipeline producing 1200×630 PNGs from a JSX-like template) and `src/pages/og/[...slug].png.ts` endpoint (enumerates all blog posts, neighborhoods, and static pages into `dist/og/<slug>.png`). Three template variants: **blog** (title + category badge), **neighborhood** (title + county badge), **default** (title only). Design approved against Pillow mockup on 2026-05-18: diagonal navy gradient (`#001825 → #002a3a`), Selling 303 wordmark top-left, headshot bleeding bottom-right, DM Serif Display title left-aligned, gold (`#c8965a`) Inter SemiBold badge. Brand consistency vetted against brand-style-guide.
  - **New dependencies:** `satori@^0.12.2`, `@resvg/resvg-js@^2.6.2`. Netlify installs at build.
  - **New assets:** `src/assets/og/jacob-stark_headshot_800x800.png` (transparent-bg headshot), `src/assets/og/fonts/` (TTF copies of DM Serif Display + Inter SemiBold — Satori does NOT support WOFF2 so the site-wide `public/fonts/*.woff2` set was unusable here). `src/assets/selling303-logo/` populated by design system on 2026-05-18 with `-on-light` / `-on-dark` naming convention (the legacy `-light` / `-dark` files remain — they describe background context, not logo color).
  - **SEO.astro auto-derives `og:image`** from the page path (`/og/<path>.png`) when no override is passed. Dynamic routes (`blog/[slug]`, `neighborhoods/[slug]`) pass explicit per-page URLs. Static pages just dropped their broken explicit `ogImage` prop.
  - **Schema.org canonical asset URLs restored.** `public/images/selling303-logo.png` + `public/images/jacob-stark-headshot.png` now exist as real ImageObject targets for the homepage RealEstateAgent + Organization schema.
  - **Sitemap redirect added.** `/sitemap.xml → /sitemap-index.xml 301` in `public/_redirects` (Astro emits the latter; the former 404'd, breaking GSC and external services that hit the legacy path).
  - **Files removed:** every `ogImage="https://selling303.com/images/og-*.jpg"` line across 18 pages (the broken refs). Files modified: `src/components/SEO.astro`, `src/pages/blog/[slug].astro` (2 spots — Article schema image + ogImage const), `src/pages/neighborhoods/[slug].astro`, plus the 18 static pages with stripped legacy props.
  - **Not in this commit (deferred):** Chunk B (off-site SKILL.md case-fixes + `~/.claude/skills` reference in seo-aeo-weekly-audit) and Chunk C (delete dead root-level twins — `_redirects`, `_headers`, favicons, `robots.txt`, `sitemap.xml`, `css/`, `js/`, `images/`, etc.). Both planned as follow-ups after this chunk verifies on `live`.

- **2026-05-17 [SEO/AEO — Review schema completeness on success stories + homepage]:** Audit of GSC's "Item Name N/A" column on Breadcrumb / FAQ / Review reports surfaced real gaps in Review markup (the BreadcrumbList/FAQPage N/A is harmless; the Review side had actual issues). Two fixes in this commit. (1) **`src/pages/sell/success-stories/[slug].astro` — LocalBusiness wrapper completeness.** Previous schema had `name`, `@id`, `review[]`, `aggregateRating` only — missing the fields Google requires for Review rich result snippet eligibility on a LocalBusiness wrapper. Added `url`, `telephone` (+1-303-997-0634), `image` (Jacob's headshot), `priceRange` ($$$), `address` (8z Real Estate office in Greenwood Village), and `openingHoursSpecification` (Mon-Sun 08:00-20:00). Per-Review enrichment added `datePublished` (from frontmatter `closeDate`), `itemReviewed` (@id ref to LocalBusiness), and `publisher` (@id ref to Organization). Net: 15 success-story pages × ~2 testimonials each = ~30 Review entries now Google-eligible for star-rating rich result display in search results. (2) **`src/pages/index.astro` — homepage Review array backing the existing AggregateRating.** Existing homepage RealEstateAgent block already had `aggregateRating` (5.0, reviewCount 47) but no `review` array to back it. Added a second JSON-LD block with same `@id` (`https://selling303.com/#agent`) carrying a `review` array dynamically built from all published success stories' testimonials (filtered via `getCollection('successStories', e => !e.data.draft)`). Each homepage Review carries the same datePublished + itemReviewed + publisher fields. Google merges multiple JSON-LD blocks by `@id`, so the agent profile now has both aggregate AND backing reviews on the homepage. Verified: all 15 success-story md files have `closeDate` in frontmatter. Frontmatter JS syntax-checked clean. Build verification on next Netlify deploy.
