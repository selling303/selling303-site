---
import BaseLayout from '../layouts/BaseLayout.astro';
import NeighborhoodCard from '../components/NeighborhoodCard.astro';
import { getImage } from 'astro:assets';

import nbLittletonSrc from '../assets/images/neighborhood-littleton.jpg';
import nbHighlandsRanchSrc from '../assets/images/neighborhood-highlands-ranch.jpg';
import nbCentennialSrc from '../assets/images/neighborhood-centennial.jpg';
import nbEnglewoodSrc from '../assets/images/neighborhood-englewood.jpg';
import nbParkerSrc from '../assets/images/neighborhood-parker.jpg';
import nbLakewoodSrc from '../assets/images/neighborhood-lakewood.jpg';

const nbLittleton = await getImage({ src: nbLittletonSrc, format: 'webp', quality: 80, width: 800 });
const nbHighlandsRanch = await getImage({ src: nbHighlandsRanchSrc, format: 'webp', quality: 80, width: 800 });
const nbCentennial = await getImage({ src: nbCentennialSrc, format: 'webp', quality: 80, width: 800 });
const nbEnglewood = await getImage({ src: nbEnglewoodSrc, format: 'webp', quality: 80, width: 800 });
const nbParker = await getImage({ src: nbParkerSrc, format: 'webp', quality: 80, width: 800 });
const nbLakewood = await getImage({ src: nbLakewoodSrc, format: 'webp', quality: 80, width: 800 });
---

<BaseLayout headerVariant="solid" title="Neighborhoods | Selling 303 — South Denver Metro Communities" description="Explore South Denver Metro neighborhoods — Littleton, Highlands Ranch, Centennial, Englewood, Parker, Lakewood, Lone Tree, Castle Pines, and Greenwood Village. Market stats, lifestyle info, and active listings." currentPage="neighborhoods">
  <Fragment slot="head">
    <!-- RealScout widget -->
    <script src="https://em.realscout.com/widgets/realscout-web-components.umd.js" type="module" is:inline></script>

    <!-- Schema: WebPage -->
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "WebPage",
      "name": "South Denver Metro Neighborhoods | Selling 303",
      "url": "https://selling303.com/neighborhoods",
      "description": "Explore South Denver Metro neighborhoods — market stats, lifestyle info, and active listings.",
      "author": {"@type": "Person", "name": "Jacob Stark"}
    }
    </script>

    <style>
      /* ── Match Me Quiz ── */
      .quiz-section {
        background: var(--color-primary);
        padding: var(--space-3xl) 0;
      }
      .quiz-section .section-label {
        color: var(--color-accent);
      }
      .quiz-section h2 {
        color: #fff;
        font-family: var(--font-heading);
        font-size: clamp(1.75rem, 4vw, 2.5rem);
        margin-bottom: var(--space-sm);
      }
      .quiz-section .quiz-intro {
        color: rgba(255,255,255,0.65);
        margin-bottom: var(--space-xl);
        max-width: 560px;
      }

      .quiz-card {
        background: rgba(255,255,255,0.06);
        border: 1px solid rgba(255,255,255,0.12);
        border-radius: 16px;
        padding: var(--space-xl) var(--space-xl);
        max-width: 680px;
      }

      .quiz-step { display: none; }
      .quiz-step.active { display: block; }

      .quiz-progress {
        display: flex;
        gap: 8px;
        margin-bottom: var(--space-lg);
      }
      .quiz-progress__dot {
        width: 8px;
        height: 8px;
        border-radius: 50%;
        background: rgba(255,255,255,0.2);
        transition: background 0.2s;
      }
      .quiz-progress__dot.filled { background: var(--color-accent); }

      .quiz-question {
        font-size: 1.1rem;
        font-weight: 600;
        color: #fff;
        margin-bottom: var(--space-md);
      }

      .quiz-options {
        display: flex;
        flex-direction: column;
        gap: 10px;
      }
      .quiz-option {
        background: rgba(255,255,255,0.05);
        border: 1.5px solid rgba(255,255,255,0.15);
        border-radius: 10px;
        padding: 12px 16px;
        color: rgba(255,255,255,0.85);
        font-size: 0.92rem;
        cursor: pointer;
        text-align: left;
        transition: border-color 0.15s, background 0.15s;
        width: 100%;
      }
      .quiz-option:hover {
        border-color: var(--color-accent);
        background: rgba(200,150,90,0.1);
      }
      .quiz-option.selected {
        border-color: var(--color-accent);
        background: rgba(200,150,90,0.15);
        color: #fff;
      }

      .quiz-nav {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-top: var(--space-lg);
        gap: var(--space-sm);
      }
      .quiz-btn-back {
        background: transparent;
        border: 1.5px solid rgba(255,255,255,0.2);
        color: rgba(255,255,255,0.6);
        border-radius: 8px;
        padding: 10px 20px;
        font-size: 0.88rem;
        cursor: pointer;
        transition: border-color 0.15s, color 0.15s;
      }
      .quiz-btn-back:hover {
        border-color: rgba(255,255,255,0.5);
        color: #fff;
      }
      .quiz-btn-next {
        background: var(--color-accent);
        border: none;
        color: #fff;
        border-radius: 8px;
        padding: 10px 24px;
        font-size: 0.88rem;
        font-weight: 600;
        cursor: pointer;
        transition: opacity 0.15s;
      }
      .quiz-btn-next:disabled {
        opacity: 0.35;
        cursor: not-allowed;
      }

      /* Results */
      .quiz-results { display: none; }
      .quiz-results.active { display: block; }
      .quiz-results__heading {
        font-size: 1rem;
        color: rgba(255,255,255,0.65);
        margin-bottom: var(--space-md);
      }
      .quiz-results__picks {
        display: flex;
        flex-wrap: wrap;
        gap: 10px;
        margin-bottom: var(--space-lg);
      }
      .quiz-result-chip {
        background: var(--color-accent);
        color: #fff;
        font-weight: 700;
        font-size: 0.85rem;
        border-radius: 24px;
        padding: 8px 18px;
      }
      .quiz-results__note {
        color: rgba(255,255,255,0.55);
        font-size: 0.85rem;
        margin-top: var(--space-sm);
      }
      .quiz-restart-btn {
        background: transparent;
        border: 1.5px solid rgba(255,255,255,0.25);
        color: rgba(255,255,255,0.6);
        border-radius: 8px;
        padding: 8px 18px;
        font-size: 0.82rem;
        cursor: pointer;
        margin-top: var(--space-md);
        transition: border-color 0.15s, color 0.15s;
      }
      .quiz-restart-btn:hover {
        border-color: rgba(255,255,255,0.5);
        color: #fff;
      }

      /* ── Neighborhood Cards ── */
      .neighborhoods-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
        gap: var(--space-lg);
      }

      /* Card styles live in NeighborhoodCard.astro — do not duplicate here */

      /* ── Matched section header ── */
      .match-header {
        display: none;
        margin-bottom: var(--space-md);
        padding: var(--space-md) var(--space-lg);
        background: rgba(200,150,90,0.08);
        border: 1px solid rgba(200,150,90,0.25);
        border-radius: 10px;
      }
      .match-header.visible { display: block; }
      .match-header p {
        font-size: 0.9rem;
        color: var(--color-text);
        margin: 0;
      }
      .match-header strong { color: var(--color-accent); }

      /* ── Service area map ── */
      .service-area {
        background: var(--color-primary);
        padding: var(--space-3xl) 0;
      }
      .service-area .section-label {
        color: var(--color-accent);
      }
      .service-area h2 {
        color: #fff;
        font-family: var(--font-heading);
        font-size: clamp(1.5rem, 3vw, 2rem);
        margin-bottom: var(--space-sm);
      }
      .service-area__intro {
        color: rgba(255,255,255,0.6);
        max-width: 580px;
        margin-bottom: var(--space-2xl);
      }
      .service-area__grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
        gap: var(--space-sm);
      }
      .service-area__city {
        background: rgba(255,255,255,0.05);
        border: 1px solid rgba(255,255,255,0.1);
        border-radius: 10px;
        padding: var(--space-md) var(--space-lg);
      }
      .service-area__city-name {
        font-weight: 700;
        color: #fff;
        font-size: 0.95rem;
        margin-bottom: 4px;
      }
      .service-area__city-context {
        font-size: 0.78rem;
        color: rgba(255,255,255,0.45);
        line-height: 1.4;
      }
      .service-area__map-note {
        margin-top: var(--space-xl);
        color: rgba(255,255,255,0.3);
        font-size: 0.8rem;
        font-style: italic;
      }
    </style>
  </Fragment>

  <!-- PAGE HERO -->
  <section class="page-hero">
    <div class="container">
      <span class="section-label">South Denver Metro</span>
      <h1>Find Your Neighborhood</h1>
      <p>Answer three quick questions and we'll match you to the South Denver communities that fit your life — or browse all nine below.</p>
    </div>
  </section>

  <!-- MATCH ME QUIZ -->
  <section class="quiz-section" id="quiz">
    <div class="container">
      <span class="section-label">Match Me</span>
      <h2>Which South Denver Neighborhood Is Right for You?</h2>
      <p class="quiz-intro">Three questions. Real recommendations based on what actually matters to buyers in this market.</p>

      <div class="quiz-card">
        <!-- Step 1 -->
        <div class="quiz-step active" id="step-1">
          <div class="quiz-progress">
            <div class="quiz-progress__dot filled" id="dot-1"></div>
            <div class="quiz-progress__dot" id="dot-2"></div>
            <div class="quiz-progress__dot" id="dot-3"></div>
          </div>
          <p class="quiz-question">What matters most to you in a location?</p>
          <div class="quiz-options" id="q1-options">
            <button class="quiz-option" data-q="q1" data-val="commute-dtc">Short commute to the DTC</button>
            <button class="quiz-option" data-q="q1" data-val="mountain-access">Mountain access & outdoor lifestyle</button>
            <button class="quiz-option" data-q="q1" data-val="walkable-downtown">Walkable downtown & local shops</button>
            <button class="quiz-option" data-q="q1" data-val="space-privacy">Space, privacy, and room to spread out</button>
            <button class="quiz-option" data-q="q1" data-val="newer-construction">Newer construction and modern amenities</button>
          </div>
          <div class="quiz-nav">
            <span></span>
            <button class="quiz-btn-next" id="next-1" disabled>Next →</button>
          </div>
        </div>

        <!-- Step 2 -->
        <div class="quiz-step" id="step-2">
          <div class="quiz-progress">
            <div class="quiz-progress__dot filled"></div>
            <div class="quiz-progress__dot filled" id="dot-2b"></div>
            <div class="quiz-progress__dot" id="dot-3b"></div>
          </div>
          <p class="quiz-question">What's your budget for a single-family home?</p>
          <div class="quiz-options" id="q2-options">
            <button class="quiz-option" data-q="q2" data-val="under-650k">Under $650K</button>
            <button class="quiz-option" data-q="q2" data-val="650k-750k">$650K – $750K</button>
            <button class="quiz-option" data-q="q2" data-val="750k-950k">$750K – $950K</button>
            <button class="quiz-option" data-q="q2" data-val="950k-plus">$950K+</button>
          </div>
          <div class="quiz-nav">
            <button class="quiz-btn-back" id="back-2">← Back</button>
            <button class="quiz-btn-next" id="next-2" disabled>Next →</button>
          </div>
        </div>

        <!-- Step 3 -->
        <div class="quiz-step" id="step-3">
          <div class="quiz-progress">
            <div class="quiz-progress__dot filled"></div>
            <div class="quiz-progress__dot filled"></div>
            <div class="quiz-progress__dot filled"></div>
          </div>
          <p class="quiz-question">What's your vibe?</p>
          <div class="quiz-options" id="q3-options">
            <button class="quiz-option" data-q="q3" data-val="established-character">Established neighborhoods with real character</button>
            <button class="quiz-option" data-q="q3" data-val="master-planned">Master-planned with trails, rec centers, and HOA perks</button>
            <button class="quiz-option" data-q="q3" data-val="small-town">Small-town feel without the long drive to Denver</button>
            <button class="quiz-option" data-q="q3" data-val="luxury-exclusive">Luxury, exclusivity, and prestige address</button>
          </div>
          <div class="quiz-nav">
            <button class="quiz-btn-back" id="back-3">← Back</button>
            <button class="quiz-btn-next" id="next-3" disabled>See My Matches →</button>
          </div>
        </div>

        <!-- Results -->
        <div class="quiz-results" id="quiz-results">
          <div class="quiz-progress">
            <div class="quiz-progress__dot filled"></div>
            <div class="quiz-progress__dot filled"></div>
            <div class="quiz-progress__dot filled"></div>
          </div>
          <p class="quiz-results__heading">Based on your answers, here are your top matches — highlighted below:</p>
          <div class="quiz-results__picks" id="results-chips"></div>
          <p class="quiz-results__note">Scroll down to explore your matches in detail, or browse all nine neighborhoods.</p>
          <button class="quiz-restart-btn" id="restart-quiz">↻ Start over</button>
        </div>
      </div>
    </div>
  </section>

  <!-- NEIGHBORHOOD CARDS -->
  <section class="section section--white" id="neighborhoods">
    <div class="container">
      <span class="section-label">All Communities</span>
      <h2 class="section-heading">Explore the South Denver Metro</h2>
      <p class="section-subtext">Market data, lifestyle details, and active listings for the communities where families buy and sell with us. Stats: DMAR / REcolorado MLS, Q1 2026.</p>

      <div class="match-header" id="match-header">
        <p>Your quiz results are highlighted. <strong id="match-names"></strong> best match your priorities — but all nine are worth a look.</p>
      </div>

      <div class="neighborhoods-grid" id="neighborhoods-grid">

        <!-- Littleton -->
        <NeighborhoodCard variant="full" name="Littleton" slug="littleton" county="Arapahoe County"
          description="Littleton is the Saturday-morning-farmers-market town — a walkable Main Street, light rail, and mountain views without losing your Denver connection."
          medianPrice={703000} medianPriceDisplay="$703,000" dom={51}
          imageSrc={nbLittleton.src} imageAlt="Suburban street in Littleton, Colorado"
          searchUrl="https://selling303.realscout.com/search?city=Littleton" />

        <!-- Highlands Ranch -->
        <NeighborhoodCard variant="full" name="Highlands Ranch" slug="highlands-ranch" county="Douglas County"
          description="Highlands Ranch is the family command center — 70+ miles of trails, four rec centers, top-rated schools, and resale values that hold."
          medianPrice={718500} medianPriceDisplay="$718,500" dom={52}
          imageSrc={nbHighlandsRanch.src} imageAlt="Modern home in Highlands Ranch, Colorado"
          searchUrl="https://selling303.realscout.com/search?city=Highlands+Ranch" />

        <!-- Centennial -->
        <NeighborhoodCard variant="full" name="Centennial" slug="centennial" county="Arapahoe County"
          description="Centennial is where DTC commuters put down roots — central, established, larger lots, and quietly appreciating year over year."
          medianPrice={699000} medianPriceDisplay="$699,000" dom={48}
          imageSrc={nbCentennial.src} imageAlt="Residential home in Centennial, Colorado"
          searchUrl="https://selling303.realscout.com/search?city=Centennial" />

        <!-- Englewood -->
        <NeighborhoodCard variant="full" name="Englewood" slug="englewood" county="Arapahoe County"
          description="Englewood is South Denver's best-kept secret — light rail, walkable pockets, and the lowest entry point in the metro with room to grow."
          medianPrice={655800} medianPriceDisplay="$655,800" dom={60}
          imageSrc={nbEnglewood.src} imageAlt="Residential neighborhood in Englewood, Colorado"
          searchUrl="https://selling303.realscout.com/search?city=Englewood" />

        <!-- Parker -->
        <NeighborhoodCard variant="full" name="Parker" slug="parker" county="Douglas County"
          description="Parker is the small town that didn't get swallowed by the suburbs — an active Main Street, elbow room, newer homes, and a real sense of community."
          medianPrice={706324} medianPriceDisplay="$706,324" dom={62}
          imageSrc={nbParker.src} imageAlt="Suburban home in Parker, Colorado"
          searchUrl="https://selling303.realscout.com/search?city=Parker" />

        <!-- Lakewood -->
        <NeighborhoodCard variant="full" name="Lakewood" slug="lakewood" county="Jefferson County"
          description="Lakewood is your gateway to Red Rocks and the foothills — without leaving Denver behind. Diverse housing stock at the metro's most accessible price point."
          medianPrice={636900} medianPriceDisplay="$636,900" dom={39}
          imageSrc={nbLakewood.src} imageAlt="Mountain foothills view near Lakewood, Colorado"
          searchUrl="https://selling303.realscout.com/search?city=Lakewood" />

        <!-- Lone Tree (Unsplash placeholder — swap for real photo when available) -->
        <NeighborhoodCard variant="full" name="Lone Tree" slug="lone-tree" county="Douglas County"
          description="Lone Tree is where executive living meets light rail — Park Meadows, RidgeGate, Lincoln Station, and a fast commute to anywhere in the metro."
          medianPrice={918485} medianPriceDisplay="$918,485" dom={61}
          imageSrc="https://images.unsplash.com/photo-1613490493576-7fde63acd811?w=800&h=500&fit=crop&q=80"
          imageAlt="Modern home in Lone Tree, Colorado"
          searchUrl="https://selling303.realscout.com/search?city=Lone+Tree" />

        <!-- Castle Pines (Unsplash placeholder — swap for real photo when available) -->
        <NeighborhoodCard variant="full" name="Castle Pines" slug="castle-pines" county="Douglas County"
          description="Castle Pines is the neighborhood where the pine trees are real, the golf isn't a metaphor, and the DTC is still only 25 minutes away."
          medianPrice={959000} medianPriceDisplay="$959,000" dom={77}
          imageSrc="https://images.unsplash.com/photo-1600047509807-ba8f99d2cdde?w=800&h=500&fit=crop&q=80"
          imageAlt="Large home among pine trees in Castle Pines, Colorado"
          searchUrl="https://selling303.realscout.com/search?city=Castle+Pines" />

        <!-- Greenwood Village (Unsplash placeholder — swap for real photo when available) -->
        <NeighborhoodCard variant="full" name="Greenwood Village" slug="greenwood-village" county="Arapahoe County"
          description="Greenwood Village is old-money South Denver — large lots, landmark estates, and a DTC address that speaks for itself at the metro's highest price point."
          medianPrice={1847000} medianPriceDisplay="$1,847,000" dom={40}
          imageSrc="https://images.unsplash.com/photo-1600585154526-990dced4db0d?w=800&h=500&fit=crop&q=80"
          imageAlt="Luxury home in Greenwood Village, Colorado"
          searchUrl="https://selling303.realscout.com/search?city=Greenwood+Village" />

      </div>
    </div>
  </section>

  <!-- CTA BANNER -->
  <section class="cta-banner">
    <div class="container">
      <h2>Still Not Sure Which Area Fits?</h2>
      <p>Tell me your priorities — commute, budget, lifestyle — and I'll give you an honest recommendation based on the data.</p>
      <a href="https://calendly.com/jacob-realtor" class="btn btn--primary btn--lg" target="_blank" rel="noopener">Schedule a Free Call</a>
    </div>
  </section>

  <!-- SERVICE AREA -->
  <section class="service-area" id="service-area">
    <div class="container">
      <span class="section-label">Where We Work</span>
      <h2>Our South Denver Service Area</h2>
      <p class="service-area__intro">Jacob Stark serves buyers and sellers across nine South Denver Metro communities — from Jefferson County in the west to Douglas County in the south, and Arapahoe County in between.</p>

      <div class="service-area__grid">
        <div class="service-area__city">
          <div class="service-area__city-name">Littleton</div>
          <div class="service-area__city-context">Arapahoe & Jefferson County. Along the South Platte River corridor, C-470 and US-85.</div>
        </div>
        <div class="service-area__city">
          <div class="service-area__city-name">Highlands Ranch</div>
          <div class="service-area__city-context">Douglas County. South of Lone Tree, anchored by the HRCA rec centers and E-470.</div>
        </div>
        <div class="service-area__city">
          <div class="service-area__city-name">Centennial</div>
          <div class="service-area__city-context">Arapahoe County. Bordered by Greenwood Village (north), Parker (east), and Highlands Ranch (south).</div>
        </div>
        <div class="service-area__city">
          <div class="service-area__city-name">Englewood</div>
          <div class="service-area__city-context">Arapahoe County. Immediately south of Denver, close-in with light rail running through Broadway.</div>
        </div>
        <div class="service-area__city">
          <div class="service-area__city-name">Parker</div>
          <div class="service-area__city-context">Douglas County. Southeast of the metro along E-470, adjacent to Castle Rock.</div>
        </div>
        <div class="service-area__city">
          <div class="service-area__city-name">Lakewood</div>
          <div class="service-area__city-context">Jefferson County. West side of the metro, bordering Denver and running up to the foothills near Red Rocks.</div>
        </div>
        <div class="service-area__city">
          <div class="service-area__city-name">Lone Tree</div>
          <div class="service-area__city-context">Douglas County. Just north of Highlands Ranch, anchored by Park Meadows and Lincoln Station light rail.</div>
        </div>
        <div class="service-area__city">
          <div class="service-area__city-name">Castle Pines</div>
          <div class="service-area__city-context">Douglas County. South of Lone Tree, east of Castle Rock, surrounded by the namesake pine forests.</div>
        </div>
        <div class="service-area__city">
          <div class="service-area__city-name">Greenwood Village</div>
          <div class="service-area__city-context">Arapahoe County. Sits inside the Denver Tech Center, bordered by Centennial, Englewood, and Cherry Hills Village.</div>
        </div>
      </div>

      <p class="service-area__map-note">Interactive map coming soon — for now, schedule a call and Jacob will walk you through exactly where each community sits relative to your workplace and priorities.</p>
    </div>
  </section>

  <script is:inline>
  (function () {
    // Scoring map: each answer value → { neighborhoodId: points }
    var scoring = {
      q1: {
        'commute-dtc':        { centennial: 3, englewood: 2, 'greenwood-village': 2, 'lone-tree': 1 },
        'mountain-access':    { lakewood: 3, 'castle-pines': 2, littleton: 1 },
        'walkable-downtown':  { littleton: 3, parker: 2, englewood: 1 },
        'space-privacy':      { 'castle-pines': 3, parker: 2, 'highlands-ranch': 1 },
        'newer-construction': { 'highlands-ranch': 2, 'lone-tree': 2, parker: 2, centennial: 1 }
      },
      q2: {
        'under-650k':  { englewood: 3, lakewood: 3 },
        '650k-750k':   { littleton: 2, centennial: 2, englewood: 1, parker: 2, lakewood: 1 },
        '750k-950k':   { 'highlands-ranch': 2, 'lone-tree': 2, parker: 1, centennial: 1 },
        '950k-plus':   { 'castle-pines': 3, 'greenwood-village': 3, 'lone-tree': 1 }
      },
      q3: {
        'established-character': { littleton: 3, englewood: 2, centennial: 1 },
        'master-planned':        { 'highlands-ranch': 3, 'lone-tree': 2 },
        'small-town':            { parker: 3, littleton: 2 },
        'luxury-exclusive':      { 'greenwood-village': 3, 'castle-pines': 2, 'lone-tree': 1 }
      }
    };

    var displayNames = {
      'littleton': 'Littleton',
      'highlands-ranch': 'Highlands Ranch',
      'centennial': 'Centennial',
      'englewood': 'Englewood',
      'parker': 'Parker',
      'lakewood': 'Lakewood',
      'lone-tree': 'Lone Tree',
      'castle-pines': 'Castle Pines',
      'greenwood-village': 'Greenwood Village'
    };

    var answers = { q1: null, q2: null, q3: null };

    // Option selection
    document.addEventListener('click', function (e) {
      var btn = e.target.closest('.quiz-option');
      if (!btn) return;
      var q = btn.dataset.q;
      var container = btn.closest('.quiz-options');
      container.querySelectorAll('.quiz-option').forEach(function (b) { b.classList.remove('selected'); });
      btn.classList.add('selected');
      answers[q] = btn.dataset.val;
      // Enable next button for this step
      var stepNum = q.replace('q', '');
      var nextBtn = document.getElementById('next-' + stepNum);
      if (nextBtn) nextBtn.disabled = false;
    });

    // Navigation
    function showStep(n) {
      document.querySelectorAll('.quiz-step').forEach(function (s) { s.classList.remove('active'); });
      var step = document.getElementById('step-' + n);
      if (step) step.classList.add('active');
    }

    document.getElementById('next-1').addEventListener('click', function () {
      if (!answers.q1) return;
      showStep(2);
    });
    document.getElementById('back-2').addEventListener('click', function () { showStep(1); });
    document.getElementById('next-2').addEventListener('click', function () {
      if (!answers.q2) return;
      showStep(3);
    });
    document.getElementById('back-3').addEventListener('click', function () { showStep(2); });
    document.getElementById('next-3').addEventListener('click', function () {
      if (!answers.q3) return;
      showResults();
    });
    document.getElementById('restart-quiz').addEventListener('click', function () {
      answers = { q1: null, q2: null, q3: null };
      document.querySelectorAll('.quiz-option').forEach(function (b) { b.classList.remove('selected'); });
      ['next-1','next-2','next-3'].forEach(function (id) {
        var btn = document.getElementById(id);
        if (btn) btn.disabled = true;
      });
      document.querySelectorAll('.quiz-step').forEach(function (s) { s.classList.remove('active'); });
      document.getElementById('quiz-results').classList.remove('active');
      document.getElementById('step-1').classList.add('active');
      clearHighlights();
    });

    function showResults() {
      // Tally scores
      var scores = {};
      Object.keys(displayNames).forEach(function (id) { scores[id] = 0; });
      ['q1','q2','q3'].forEach(function (q) {
        var val = answers[q];
        if (!val || !scoring[q][val]) return;
        var map = scoring[q][val];
        Object.keys(map).forEach(function (id) { scores[id] += map[id]; });
      });

      // Sort and pick top 3
      var sorted = Object.keys(scores).sort(function (a, b) { return scores[b] - scores[a]; });
      var top = sorted.slice(0, 3).filter(function (id) { return scores[id] > 0; });

      // Show results panel
      document.querySelectorAll('.quiz-step').forEach(function (s) { s.classList.remove('active'); });
      var resultsEl = document.getElementById('quiz-results');
      resultsEl.classList.add('active');

      var chipsEl = document.getElementById('results-chips');
      chipsEl.innerHTML = '';
      top.forEach(function (id) {
        var chip = document.createElement('span');
        chip.className = 'quiz-result-chip';
        chip.textContent = displayNames[id];
        chipsEl.appendChild(chip);
      });

      // Highlight cards
      highlightCards(top);

      // Scroll to neighborhoods section
      setTimeout(function () {
        var nbSection = document.getElementById('neighborhoods');
        if (nbSection) nbSection.scrollIntoView({ behavior: 'smooth', block: 'start' });
      }, 400);
    }

    function highlightCards(topIds) {
      var cards = document.querySelectorAll('.nbhd-card');
      cards.forEach(function (card) {
        var id = card.dataset.id;
        if (topIds.indexOf(id) !== -1) {
          card.classList.add('highlighted');
        } else {
          card.classList.remove('highlighted');
        }
      });

      // Reorder: highlighted first
      var grid = document.getElementById('neighborhoods-grid');
      var highlighted = [], rest = [];
      cards.forEach(function (card) {
        if (card.classList.contains('highlighted')) highlighted.push(card);
        else rest.push(card);
      });
      highlighted.concat(rest).forEach(function (card) { grid.appendChild(card); });

      // Show match header
      var header = document.getElementById('match-header');
      var namesEl = document.getElementById('match-names');
      header.classList.add('visible');
      namesEl.textContent = topIds.map(function (id) { return displayNames[id]; }).join(', ');
    }

    function clearHighlights() {
      document.querySelectorAll('.nbhd-card').forEach(function (card) {
        card.classList.remove('highlighted');
      });
      document.getElementById('match-header').classList.remove('visible');
    }
  })();
  </script>
</BaseLayout>
